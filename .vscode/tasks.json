{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dotnet run (aktuelles Projekt)",
            "type": "shell",
            "command": "dotnet run",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}
